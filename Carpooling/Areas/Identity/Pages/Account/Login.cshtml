@page
@model LoginModel
<head>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" />
    <link rel="stylesheet" href="~/css/loginStyles.css" />

</head>
<body>
    <form method="post" class="formulario">
        <h4>Iniciar sesión</h4>
        <div class="contenedor">
            <div class="input-contenedor">
                <i class="fas fa-envelope icon"></i>
                <input asp-for="Input.Email" placeholder="Correo Electrónico" type="text" />
            </div>
            <div class="input-contenedor">
                <i class="fas fa-key icon"></i>
                <input asp-for="Input.Password" placeholder="Contraseña" type="password" />
            </div>
            <input type="submit" value="Iniciar sesión" class="button" />
            <p>¿No tienes cuenta?<a class="link" href="/Identity/Account/Register"> Registrate aquí</a></p>
        </div>
    </form>
    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>